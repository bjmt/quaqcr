# Add a force=TRUE option for when non-identical params were used.
# Check that $params$integer, $params$boolean, and $genome are identical.

merge_reports <- function(x, check.params = TRUE, check.genomes = TRUE) {

}

